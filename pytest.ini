[pytest]
; Only collect from tests/. The repo root carries manual dev harnesses
; named test_*.py (SOUL audio demos) that run module-level code and
; sys.exit when their fixtures are missing — bare pytest collection
; imported them and aborted the whole CI run before any test ran.
testpaths = tests
